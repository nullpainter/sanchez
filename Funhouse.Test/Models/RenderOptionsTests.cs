using Funhouse.Models;
using NUnit.Framework;

namespace Funhouse.Test.Models
{
    [TestFixture(TestOf = typeof(RenderOptions))]
    public class RenderOptionsTests 
    {
        [Test]
        public void WRiteme()
        {
            Assert.Fail();
        }
    }
}